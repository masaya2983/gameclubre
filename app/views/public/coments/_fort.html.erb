<%= form_with(model:[game,comment],local: false) do |f| %>
  <%= f.text_area :commment, rows:'5',placeholder: "コメントをここに", class:"w-100 comment-textarea" %>
  <%= f.submit "送信する",class: "btnbtn-lg btn-base-1 mt-20 pull-right" %>
<% end %>