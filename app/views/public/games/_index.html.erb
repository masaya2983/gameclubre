<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>

      <th colspan="3"></th>
     </tr>
  </thead>
  <tbody>
   <% games.each do |game| %>
    <tr>
      <td><%= link_to(game.user) do %>
        <%= image_tag game.user.get_profile_image, '50×50' %>
        <% end %>
      </td>
      <td><%=link_to game.title,game, class: "game_#{game.id}" %></td>
      <td><%= link_to game.category_id%></td>
      <td><%= link_to game.star%></td>
      <td><%= game.review %></td>
      <td class=<%= "favorite-" + game.id.to_s %> >
       <%= render "favorites/btn",game:game %>
       </td>
        <td><%= render "public/comments/counter", game: game %></td>
   　 </tr>
  　<% end %>
　</tbody>
</table>